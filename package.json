{
  "name": "frameworks-overhead-comparison",
  "type": "module",
  "scripts": {
    "chart": "node scripts/generate-chart.js",
    "measure": "node scripts/measure-bundles.js && node scripts/generate-chart.js"
  }
}
